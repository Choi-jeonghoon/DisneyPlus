import React from 'react';
import LoginContainer from '../containers/LoginContainer';

const LoginPage = () => {
  return (
    <div>
      <LoginContainer />
    </div>
  );
};

export default LoginPage;
